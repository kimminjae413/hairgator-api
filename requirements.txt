# 렌더 최적화 requirements.txt
# 버전을 메인 파일과 일치시킴

# 웹 프레임워크 (필수)
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0

# 환경 설정
python-dotenv==1.0.0

# AI 서비스 (핵심)
openai==1.3.0
anthropic==0.7.8

# 이미지 처리 (필수)
Pillow==10.1.0

# 데이터 처리 (필수)  
pandas==2.1.3
openpyxl==3.1.2

# HTTP 클라이언트
requests==2.31.0

# 파일 처리
python-multipart==0.0.6

# Redis (메모리 모드로 폴백 가능)
redis==5.0.1