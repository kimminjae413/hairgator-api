# 헤어게이터 통합 시스템 v6.0 - 배포용 의존성 패키지

# 웹 프레임워크 (핵심)
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# 환경 설정
python-dotenv==1.0.0

# 데이터베이스 & 캐시
redis==5.0.1

# HTTP 클라이언트
requests==2.31.0

# AI 서비스 (핵심)
openai==1.3.5
anthropic==0.7.8

# 이미지 처리 (핵심)
Pillow==9.5.0

# 데이터 처리 (핵심)
pydantic==2.5.0
pandas==2.1.4
numpy==1.25.2
openpyxl==3.1.2

# 파일 처리
aiofiles==23.2.1

# 로깅 (선택사항 - 제거해도 됨)
# structlog==23.2.0

# CORS 지원 (선택사항 - FastAPI 내장)
# python-jose[cryptography]==3.3.0